import CallbackType from '../callback/type';

const Container = CallbackType.add('container');
const Dynamic = CallbackType.add('dynamic');
const Group = CallbackType.add('group');
const Kinematic = CallbackType.add('kinematic');

export { Container, Dynamic, Group, Kinematic };