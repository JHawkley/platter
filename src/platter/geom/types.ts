import CallbackType from '../callback/type';

const AABB = CallbackType.add('aabb');
const Chain = CallbackType.add('chain');
const ChainLink = CallbackType.add('chain-link');
const Circle = CallbackType.add('circle');
const Line = CallbackType.add('line');
const Point = CallbackType.add('point');

export { AABB, Chain, ChainLink, Circle, Line, Point };